diff --git a/node_modules/@antv/infographic/esm/utils/font.js b/node_modules/@antv/infographic/esm/utils/font.js
index d3672a1..bd16554 100644
--- a/node_modules/@antv/infographic/esm/utils/font.js
+++ b/node_modules/@antv/infographic/esm/utils/font.js
@@ -1,9 +1,27 @@
 export function decodeFontFamily(font) {
     return font.trim().replace(/["']/g, '');
 }
+// Fixed: handle font-family list properly instead of treating entire string as single font
 export function encodeFontFamily(font) {
     if (font.startsWith('"'))
         return font;
+    // Handle font-family list (comma-separated)
+    if (font.includes(',')) {
+        return font.split(',').map(f => {
+            f = f.trim();
+            // Already quoted with single or double quotes
+            if (f.startsWith("'") || f.startsWith('"'))
+                return f;
+            // Starts with digit
+            if (/^\d/.test(f))
+                return `"${f}"`;
+            // Contains space - needs quoting
+            if (f.includes(' '))
+                return `"${f}"`;
+            return f;
+        }).join(', ');
+    }
+    // Single font name
     if (/^\d/.test(font))
         return `"${font}"`;
     if (font.includes(' '))
diff --git a/node_modules/@antv/infographic/esm/utils/measure-text.js b/node_modules/@antv/infographic/esm/utils/measure-text.js
index 9bcffbb..add5ec7 100644
--- a/node_modules/@antv/infographic/esm/utils/measure-text.js
+++ b/node_modules/@antv/infographic/esm/utils/measure-text.js
@@ -1,7 +1,14 @@
-import { measureText as measure, registerFont } from 'measury';
-import AlibabaPuHuiTi from 'measury/fonts/AlibabaPuHuiTi-Regular';
 import { DEFAULT_FONT } from '../renderer';
-registerFont(AlibabaPuHuiTi);
+
+// Use Canvas API for text measurement to support any font
+let measureCanvas = null;
+function getCanvasContext() {
+    if (!measureCanvas) {
+        measureCanvas = document.createElement('canvas');
+    }
+    return measureCanvas.getContext('2d');
+}
+
 export function measureText(text = '', attrs) {
     if (attrs.width && attrs.height) {
         return { width: attrs.width, height: attrs.height };
@@ -9,14 +16,31 @@ export function measureText(text = '', attrs) {
     let width = 0;
     let height = 0;
     const { fontFamily = DEFAULT_FONT, fontSize = 14, fontWeight = 'normal', lineHeight = 1.4, } = attrs;
-    const metrics = measure(text.toString(), {
-        fontFamily,
-        fontSize: +fontSize,
-        fontWeight,
-        lineHeight,
-    });
-    // 额外添加 1% 宽高
-    width || (width = Math.ceil(metrics.width * 1.01));
-    height || (height = Math.ceil(metrics.height * 1.01));
+    
+    const ctx = getCanvasContext();
+    if (ctx) {
+        // Build font string for Canvas
+        ctx.font = `${fontWeight} ${fontSize}px ${fontFamily}`;
+        
+        // Handle multi-line text
+        const lines = text.toString().split('\n');
+        let maxWidth = 0;
+        for (const line of lines) {
+            const metrics = ctx.measureText(line);
+            if (metrics.width > maxWidth) {
+                maxWidth = metrics.width;
+            }
+        }
+        
+        // Calculate dimensions
+        const lineHeightPx = fontSize * lineHeight;
+        width = Math.ceil(maxWidth * 1.01);
+        height = Math.ceil(lines.length * lineHeightPx * 1.01);
+    } else {
+        // Fallback: estimate based on character count
+        width = Math.ceil(text.toString().length * fontSize * 0.6 * 1.01);
+        height = Math.ceil(fontSize * lineHeight * 1.01);
+    }
+    
     return { width, height };
 }
diff --git a/node_modules/@antv/infographic/lib/utils/font.js b/node_modules/@antv/infographic/lib/utils/font.js
index fb850f6..7c31238 100644
--- a/node_modules/@antv/infographic/lib/utils/font.js
+++ b/node_modules/@antv/infographic/lib/utils/font.js
@@ -6,9 +6,27 @@ exports.normalizeFontWeightName = normalizeFontWeightName;
 function decodeFontFamily(font) {
     return font.trim().replace(/["']/g, '');
 }
+// Fixed: handle font-family list properly instead of treating entire string as single font
 function encodeFontFamily(font) {
     if (font.startsWith('"'))
         return font;
+    // Handle font-family list (comma-separated)
+    if (font.includes(',')) {
+        return font.split(',').map(function(f) {
+            f = f.trim();
+            // Already quoted with single or double quotes
+            if (f.startsWith("'") || f.startsWith('"'))
+                return f;
+            // Starts with digit
+            if (/^\d/.test(f))
+                return `"${f}"`;
+            // Contains space - needs quoting
+            if (f.includes(' '))
+                return `"${f}"`;
+            return f;
+        }).join(', ');
+    }
+    // Single font name
     if (/^\d/.test(font))
         return `"${font}"`;
     if (font.includes(' '))
diff --git a/node_modules/@antv/infographic/lib/utils/measure-text.js b/node_modules/@antv/infographic/lib/utils/measure-text.js
index 29b3a22..d1a6197 100644
--- a/node_modules/@antv/infographic/lib/utils/measure-text.js
+++ b/node_modules/@antv/infographic/lib/utils/measure-text.js
@@ -1,13 +1,17 @@
 "use strict";
-var __importDefault = (this && this.__importDefault) || function (mod) {
-    return (mod && mod.__esModule) ? mod : { "default": mod };
-};
 Object.defineProperty(exports, "__esModule", { value: true });
 exports.measureText = measureText;
-const measury_1 = require("measury");
-const AlibabaPuHuiTi_Regular_1 = __importDefault(require("measury/fonts/AlibabaPuHuiTi-Regular"));
 const renderer_1 = require("../renderer");
-(0, measury_1.registerFont)(AlibabaPuHuiTi_Regular_1.default);
+
+// Use Canvas API for text measurement to support any font
+let measureCanvas = null;
+function getCanvasContext() {
+    if (!measureCanvas) {
+        measureCanvas = document.createElement('canvas');
+    }
+    return measureCanvas.getContext('2d');
+}
+
 function measureText(text = '', attrs) {
     if (attrs.width && attrs.height) {
         return { width: attrs.width, height: attrs.height };
@@ -15,14 +19,31 @@ function measureText(text = '', attrs) {
     let width = 0;
     let height = 0;
     const { fontFamily = renderer_1.DEFAULT_FONT, fontSize = 14, fontWeight = 'normal', lineHeight = 1.4, } = attrs;
-    const metrics = (0, measury_1.measureText)(text.toString(), {
-        fontFamily,
-        fontSize: +fontSize,
-        fontWeight,
-        lineHeight,
-    });
-    // 额外添加 1% 宽高
-    width || (width = Math.ceil(metrics.width * 1.01));
-    height || (height = Math.ceil(metrics.height * 1.01));
+    
+    const ctx = getCanvasContext();
+    if (ctx) {
+        // Build font string for Canvas
+        ctx.font = `${fontWeight} ${fontSize}px ${fontFamily}`;
+        
+        // Handle multi-line text
+        const lines = text.toString().split('\n');
+        let maxWidth = 0;
+        for (const line of lines) {
+            const metrics = ctx.measureText(line);
+            if (metrics.width > maxWidth) {
+                maxWidth = metrics.width;
+            }
+        }
+        
+        // Calculate dimensions
+        const lineHeightPx = fontSize * lineHeight;
+        width = Math.ceil(maxWidth * 1.01);
+        height = Math.ceil(lines.length * lineHeightPx * 1.01);
+    } else {
+        // Fallback: estimate based on character count
+        width = Math.ceil(text.toString().length * fontSize * 0.6 * 1.01);
+        height = Math.ceil(fontSize * lineHeight * 1.01);
+    }
+    
     return { width, height };
 }
