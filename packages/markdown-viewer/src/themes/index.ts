/**
 * Themes module
 * 
 * Theme configuration and utilities
 */

import registry from './registry.json';

export { registry as themeRegistry };
export { default as fontConfig } from './font-config.json';
